name = "tab-agent-proxy"
main = "worker.ts"
compatibility_date = "2025-01-01"
dev = { port = 8787 }

# Set the key once with:  wrangler secret put OPENROUTER_API_KEY
# For local dev, put it in .dev.vars (gitignored):  OPENROUTER_API_KEY=sk-or-v1-...
# Get a free key at https://openrouter.ai/keys
