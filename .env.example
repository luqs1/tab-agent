# tab-agent has NO backend and needs no env at runtime — the OpenRouter key is
# entered in the UI and stored in localStorage. These are optional build-time
# conveniences only.

# Optional: a browser-CORS-friendly remote MCP server's Streamable HTTP endpoint.
# VITE_MCP_URL=https://your-mcp-server.example.com/mcp
